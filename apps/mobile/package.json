{
  "name": "@climbing-logbook/mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "flutter run",
    "build": "flutter build apk --release && flutter build ios --release --no-codesign",
    "test": "flutter test",
    "lint": "flutter analyze"
  }
}